"""Options section pages (scanner, paper, captured, portfolio, calculator,
swing, gamma, simulator) plus shared header strip + Trade detail panel.

Each leaf module exposes a render() that builds its page body inside the shared
shell layout (see webgui/main.py)."""

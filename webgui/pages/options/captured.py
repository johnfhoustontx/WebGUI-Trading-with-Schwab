"""Captured Signals page — placeholder (implemented in Batch B)."""
from nicegui import ui


def render():
    ui.label("Captured Signals").classes("text-h5")
    ui.label("Open signals with live marks, drift, and recommendations.").classes("opacity-70")
    ui.label("(under construction — Batch B)").classes("text-sm opacity-50")

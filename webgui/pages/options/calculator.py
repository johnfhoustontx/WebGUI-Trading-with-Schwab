"""Calculator page — placeholder (implemented in Batch B)."""
from nicegui import ui


def render():
    ui.label("Calculator").classes("text-h5")
    ui.label("Strategy P&L summary tiles and a colored P&L heatmap grid.").classes("opacity-70")
    ui.label("(under construction — Batch B)").classes("text-sm opacity-50")
